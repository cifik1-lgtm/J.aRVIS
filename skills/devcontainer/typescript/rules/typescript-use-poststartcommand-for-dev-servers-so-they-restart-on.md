---
title: "Use `postStartCommand` for dev servers so they restart on..."
impact: MEDIUM
impactDescription: "general best practice"
tags: typescript, devcontainer, typescriptnodejs-dev-container-setup, node-feature, npmpnpmyarn-configuration
---

## Use `postStartCommand` for dev servers so they restart on...

Use `postStartCommand` for dev servers so they restart on Codespace resume.
