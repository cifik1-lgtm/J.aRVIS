---
title: "Use `npm ci` or `pnpm install --frozen-lockfile` in..."
impact: MEDIUM
impactDescription: "general best practice"
tags: typescript, devcontainer, typescriptnodejs-dev-container-setup, node-feature, npmpnpmyarn-configuration
---

## Use `npm ci` or `pnpm install --frozen-lockfile` in...

Use `npm ci` or `pnpm install --frozen-lockfile` in `postCreateCommand` for deterministic installs.
