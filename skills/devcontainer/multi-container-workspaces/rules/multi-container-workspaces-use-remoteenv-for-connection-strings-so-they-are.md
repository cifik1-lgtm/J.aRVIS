---
title: "Use `remoteEnv` for connection strings so they are..."
impact: MEDIUM
impactDescription: "general best practice"
tags: multi-container-workspaces, devcontainer, docker-compose-sidecar-services, databasecachequeue-containers-alongside-dev-containers, multi-service-networking
---

## Use `remoteEnv` for connection strings so they are...

Use `remoteEnv` for connection strings so they are available in the integrated terminal.
