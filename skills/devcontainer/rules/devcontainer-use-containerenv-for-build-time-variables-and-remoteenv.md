---
title: "Use `containerEnv` for build-time variables and `remoteEnv`..."
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: devcontainer, devcontainerjson-configuration, github-codespaces-setup, lifecycle-hooks
---

## Use `containerEnv` for build-time variables and `remoteEnv`...

Use `containerEnv` for build-time variables and `remoteEnv` for secrets or connection strings.
