---
title: "Keep `.devcontainer/` in version control so every..."
impact: MEDIUM
impactDescription: "general best practice"
tags: devcontainer, devcontainerjson-configuration, github-codespaces-setup, lifecycle-hooks
---

## Keep `.devcontainer/` in version control so every...

Keep `.devcontainer/` in version control so every contributor gets the same environment.
