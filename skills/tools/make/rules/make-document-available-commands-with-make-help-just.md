---
title: "Document available commands with `make help` / `just..."
impact: MEDIUM
impactDescription: "general best practice"
tags: make, tools, makefile, just
---

## Document available commands with `make help` / `just...

Document available commands with `make help` / `just --list` / `task --list` so new developers can discover what's available
