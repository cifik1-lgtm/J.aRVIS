---
title: "Add `#!/usr/bin/env bash` shebang"
impact: MEDIUM
impactDescription: "general best practice"
tags: bash, tools, zsh, shell-scripting
---

## Add `#!/usr/bin/env bash` shebang

The `env` form is more portable across systems where Bash may not be at `/bin/bash`.
