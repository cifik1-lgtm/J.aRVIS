---
title: "Always set timeouts (`--connect-timeout` and `--max-time`)..."
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: curl, tools, wget, httpie
---

## Always set timeouts (`--connect-timeout` and `--max-time`)...

Always set timeouts (`--connect-timeout` and `--max-time`) in scripts to prevent hanging
