---
title: "Declare dependencies on other agents explicitly so the..."
impact: MEDIUM
impactDescription: "general best practice"
tags: adl, ai, declarative-agent-blueprints, agent-identity-and-permissions, llm-configuration
---

## Declare dependencies on other agents explicitly so the...

Declare dependencies on other agents explicitly so the orchestration layer knows the graph.
