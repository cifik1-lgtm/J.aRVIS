---
title: "Use `GET /agents` for runtime discovery so clients can..."
impact: MEDIUM
impactDescription: "general best practice"
tags: acp, ai, acp-agent-servers, acp-client-integration, agent-discovery-via-manifests
---

## Use `GET /agents` for runtime discovery so clients can...

Use `GET /agents` for runtime discovery so clients can dynamically route to the right agent without hardcoding endpoints.
