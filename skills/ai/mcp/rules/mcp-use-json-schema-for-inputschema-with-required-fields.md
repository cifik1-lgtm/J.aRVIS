---
title: "Use JSON Schema for `inputSchema` with `required` fields..."
impact: HIGH
impactDescription: "significant quality or reliability improvement"
tags: mcp, ai, building-mcp-tool-servers, exposing-resources-to-agents, prompt-templates
---

## Use JSON Schema for `inputSchema` with `required` fields...

Use JSON Schema for `inputSchema` with `required` fields for type safety.
