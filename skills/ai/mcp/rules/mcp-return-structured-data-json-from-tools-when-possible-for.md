---
title: "Return structured data (JSON) from tools when possible for..."
impact: MEDIUM
impactDescription: "general best practice"
tags: mcp, ai, building-mcp-tool-servers, exposing-resources-to-agents, prompt-templates
---

## Return structured data (JSON) from tools when possible for...

Return structured data (JSON) from tools when possible for downstream processing.
