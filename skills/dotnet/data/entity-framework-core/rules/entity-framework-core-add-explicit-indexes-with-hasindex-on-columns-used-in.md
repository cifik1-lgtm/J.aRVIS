---
title: "Add explicit indexes with `HasIndex` on columns used in..."
impact: HIGH
impactDescription: "significant quality or reliability improvement"
tags: entity-framework-core, dotnet, data, crud-data-access-with-linq, database-migrations, change-tracking
---

## Add explicit indexes with `HasIndex` on columns used in...

Add explicit indexes with `HasIndex` on columns used in WHERE, JOIN, and ORDER BY clauses to ensure the database engine can serve queries efficiently.
