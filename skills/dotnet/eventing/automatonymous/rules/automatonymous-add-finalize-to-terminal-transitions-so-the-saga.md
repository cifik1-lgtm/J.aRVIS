---
title: "Add `Finalize()` to terminal transitions so the saga..."
impact: MEDIUM
impactDescription: "general best practice"
tags: automatonymous, dotnet, eventing, state-machine-definitions, masstransit-saga-orchestration, orderworkflow-lifecycle-management
---

## Add `Finalize()` to terminal transitions so the saga...

Add `Finalize()` to terminal transitions so the saga instance is properly marked as completed.
