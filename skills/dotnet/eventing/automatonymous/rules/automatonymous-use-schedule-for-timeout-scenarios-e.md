---
title: "Use `Schedule` for timeout scenarios (e"
impact: MEDIUM
impactDescription: "general best practice"
tags: automatonymous, dotnet, eventing, state-machine-definitions, masstransit-saga-orchestration, orderworkflow-lifecycle-management
---

## Use `Schedule` for timeout scenarios (e

Use `Schedule` for timeout scenarios (e.g., payment not received within 30 minutes) rather than building custom timer logic.
