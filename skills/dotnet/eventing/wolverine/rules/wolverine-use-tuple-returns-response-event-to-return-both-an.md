---
title: "Use tuple returns `(Response, Event)` to return both an..."
impact: MEDIUM
impactDescription: "general best practice"
tags: wolverine, dotnet, eventing, command-and-event-handling, messaging-with-rabbitmqazure-service-busamazon-sqs, http-endpoint-generation
---

## Use tuple returns `(Response, Event)` to return both an...

Use tuple returns `(Response, Event)` to return both an HTTP response and a cascaded message from a single handler method.
