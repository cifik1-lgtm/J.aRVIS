---
title: "Inject dependencies via `Akka"
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: akka-net, dotnet, eventing, actor-based-concurrency, distributed-systems, supervision-hierarchies
---

## Inject dependencies via `Akka

Inject dependencies via `Akka.DependencyInjection` and `Props.Create` factories rather than passing services through messages.
