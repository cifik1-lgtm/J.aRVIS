---
title: "Use separate models for reads and writes"
impact: MEDIUM
impactDescription: "general best practice"
tags: command-query, dotnet, eventing
---

## Use separate models for reads and writes

Use separate models for reads and writes
