---
title: "Use `RootCommand` with descriptive text as the entry point..."
impact: MEDIUM
impactDescription: "general best practice"
tags: commandline-cheatsheet, dotnet, cli, building-cli-applications-with-typed-argument-parsing, creating-dotnet-tools-with-subcommands, systemcommandline-setup-and-configuration
---

## Use `RootCommand` with descriptive text as the entry point...

Use `RootCommand` with descriptive text as the entry point and organize functionality into `Command` subcommands rather than overloading the root with many options.
