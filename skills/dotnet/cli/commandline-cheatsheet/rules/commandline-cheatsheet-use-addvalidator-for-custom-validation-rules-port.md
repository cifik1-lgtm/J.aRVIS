---
title: "Use `AddValidator` for custom validation rules (port..."
impact: MEDIUM
impactDescription: "general best practice"
tags: commandline-cheatsheet, dotnet, cli, building-cli-applications-with-typed-argument-parsing, creating-dotnet-tools-with-subcommands, systemcommandline-setup-and-configuration
---

## Use `AddValidator` for custom validation rules (port...

Use `AddValidator` for custom validation rules (port ranges, file existence, format checks) to provide clear error messages before the handler runs.
