---
title: "Use `.Id()` with a stable, unique identifier for each..."
impact: MEDIUM
impactDescription: "general best practice"
tags: jot, dotnet, configuration, persisting-and-restoring-application-state-such-as-window-positions, user-preferences, form-values
---

## Use `.Id()` with a stable, unique identifier for each...

Use `.Id()` with a stable, unique identifier for each tracked object so that state is correctly associated when multiple instances of the same type exist.
