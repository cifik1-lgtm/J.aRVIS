---
title: "Place the root `.editorconfig` at the repository root with `root = true`"
impact: HIGH
impactDescription: "significant quality or reliability improvement"
tags: editorconfig, dotnet, project-system, configuring-consistent-c-coding-styles, naming-conventions, formatting-rules
---

## Place the root `.editorconfig` at the repository root with `root = true`

Place the root `.editorconfig` at the repository root with `root = true`: to prevent accidental inheritance from user-profile or machine-level `.editorconfig` files that exist outside your repository.
