---
title: "Use `autoReload=\"true\"`"
impact: MEDIUM
impactDescription: "general best practice"
tags: nlog, dotnet, logging, nlog-target-configuration, layout-renderers, structured-logging-with-nlog
---

## Use `autoReload="true"`

Use `autoReload="true"`: in `nlog.config` so logging configuration changes take effect without restarting the application, enabling runtime verbosity adjustments.
