---
title: "Use computation expressions (`async { }`, `task { }`,..."
impact: MEDIUM
impactDescription: "general best practice"
tags: fsharp, dotnet, functional, functional-first-net-programming, discriminated-unions-and-pattern-matching, computation-expressions
---

## Use computation expressions (`async { }`, `task { }`,...

Use computation expressions (`async { }`, `task { }`, custom `result { }`) to write monadic code that reads sequentially while handling effects implicitly.
