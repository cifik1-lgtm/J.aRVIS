---
title: "Make types and functions immutable by default; use..."
impact: MEDIUM
impactDescription: "general best practice"
tags: fsharp, dotnet, functional, functional-first-net-programming, discriminated-unions-and-pattern-matching, computation-expressions
---

## Make types and functions immutable by default; use...

Make types and functions immutable by default; use `mutable` only when interacting with C# APIs or when profiling proves a performance need.
