---
title: "Use `Select` (map) to transform the inner value and..."
impact: MEDIUM
impactDescription: "general best practice"
tags: jflepp-maybe, dotnet, functional, optional-value-handling-with-maybet, null-elimination, monadic-chaining-of-optional-values
---

## Use `Select` (map) to transform the inner value and...

Use `Select` (map) to transform the inner value and `SelectMany` (bind) to chain operations that themselves return `Maybe<T>`.
