---
title: "Use the `Curryfy` NuGet package for automatic curry/partial..."
impact: MEDIUM
impactDescription: "general best practice"
tags: curryfy, dotnet, functional, function-currying, partial-application, function-composition
---

## Use the `Curryfy` NuGet package for automatic curry/partial...

Use the `Curryfy` NuGet package for automatic curry/partial extension methods on standard `Func<>` delegates rather than implementing your own for every arity.
