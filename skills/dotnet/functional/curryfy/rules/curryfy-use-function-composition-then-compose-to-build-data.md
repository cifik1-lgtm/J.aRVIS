---
title: "Use function composition (`Then`/`Compose`) to build data..."
impact: MEDIUM
impactDescription: "general best practice"
tags: curryfy, dotnet, functional, function-currying, partial-application, function-composition
---

## Use function composition (`Then`/`Compose`) to build data...

Use function composition (`Then`/`Compose`) to build data transformation pipelines that are more readable than nested function calls.
