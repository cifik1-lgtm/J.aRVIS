---
title: "Use `.Optional()` for optional elements rather than choice..."
impact: MEDIUM
impactDescription: "general best practice"
tags: parakeet, dotnet, functional, c-peg-parser-combinators, grammar-definition, text-parsing
---

## Use `.Optional()` for optional elements rather than choice...

Use `.Optional()` for optional elements rather than choice with empty; it produces cleaner parse trees.
