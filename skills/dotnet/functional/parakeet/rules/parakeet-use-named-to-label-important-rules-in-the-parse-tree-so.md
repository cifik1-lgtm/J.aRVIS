---
title: "Use `Named()` to label important rules in the parse tree so..."
impact: HIGH
impactDescription: "significant quality or reliability improvement"
tags: parakeet, dotnet, functional, c-peg-parser-combinators, grammar-definition, text-parsing
---

## Use `Named()` to label important rules in the parse tree so...

Use `Named()` to label important rules in the parse tree so traversal code can identify semantic nodes by name rather than position.
