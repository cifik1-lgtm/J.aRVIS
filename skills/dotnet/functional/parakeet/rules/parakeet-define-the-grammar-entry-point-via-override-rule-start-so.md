---
title: "Define the grammar entry point via `override Rule Start` so..."
impact: MEDIUM
impactDescription: "general best practice"
tags: parakeet, dotnet, functional, c-peg-parser-combinators, grammar-definition, text-parsing
---

## Define the grammar entry point via `override Rule Start` so...

Define the grammar entry point via `override Rule Start` so the parser knows which rule to begin parsing from.
