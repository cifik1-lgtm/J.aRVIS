---
title: "Use `Match` with both `some` and `none` handlers as the..."
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: optional, dotnet, functional, optionalt-type-implementation, null-elimination-patterns, monadic-optional-chaining
---

## Use `Match` with both `some` and `none` handlers as the...

Use `Match` with both `some` and `none` handlers as the primary way to extract values, ensuring both cases are always handled.
