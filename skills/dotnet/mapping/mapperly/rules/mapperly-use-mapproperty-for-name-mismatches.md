---
title: "Use `[MapProperty]` for name mismatches"
impact: MEDIUM
impactDescription: "general best practice"
tags: mapperly, dotnet, mapping, high-performance-object-mapping-via-source-generation, compile-time-mapping-validation, zero-reflection-mapping
---

## Use `[MapProperty]` for name mismatches

Use `[MapProperty]` for name mismatches: instead of renaming domain or DTO properties, preserving the natural naming of each layer.
