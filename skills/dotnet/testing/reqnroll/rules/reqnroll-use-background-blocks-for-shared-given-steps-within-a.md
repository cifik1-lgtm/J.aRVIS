---
title: "Use Background blocks for shared Given steps within a feature"
impact: MEDIUM
impactDescription: "general best practice"
tags: reqnroll, dotnet, testing, behavior-driven-development-with-gherkin-syntax, writing-executable-specifications, step-definition-bindings
---

## Use Background blocks for shared Given steps within a feature

Use Background blocks for shared Given steps within a feature: extract common setup (logged-in user, seeded data) into a `Background:` section instead of repeating it in every scenario.
