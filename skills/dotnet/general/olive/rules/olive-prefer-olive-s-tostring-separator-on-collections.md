---
title: "Prefer Olive's `.ToString(separator)` on collections"
impact: LOW
impactDescription: "recommended but situational"
tags: olive, dotnet, general, common-string-extensions-null-safe-operations, validation, collection-utilities
---

## Prefer Olive's `.ToString(separator)` on collections

Prefer Olive's `.ToString(separator)` on collections: over `string.Join()` for consistency with the fluent extension method style.
