---
title: "Use `.IsValidEmail()` for quick input validation"
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: olive, dotnet, general, common-string-extensions-null-safe-operations, validation, collection-utilities
---

## Use `.IsValidEmail()` for quick input validation

Use `.IsValidEmail()` for quick input validation: but supplement with actual email delivery verification for critical flows, since format validation alone is insufficient.
