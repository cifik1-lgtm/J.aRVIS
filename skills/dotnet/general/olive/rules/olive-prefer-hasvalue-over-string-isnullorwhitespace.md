---
title: "Prefer `.HasValue()` over `!string.IsNullOrWhiteSpace()`"
impact: LOW
impactDescription: "recommended but situational"
tags: olive, dotnet, general, common-string-extensions-null-safe-operations, validation, collection-utilities
---

## Prefer `.HasValue()` over `!string.IsNullOrWhiteSpace()`

Prefer `.HasValue()` over `!string.IsNullOrWhiteSpace()`: for readability and consistency with Olive's fluent style throughout your codebase.
