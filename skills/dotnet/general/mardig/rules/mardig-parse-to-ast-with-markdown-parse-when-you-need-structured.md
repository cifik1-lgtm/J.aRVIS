---
title: "Parse to AST with `Markdown.Parse()` when you need structured access"
impact: MEDIUM
impactDescription: "general best practice"
tags: mardig, dotnet, general, converting-markdown-to-html, building-custom-markdown-pipelines, parsing-markdown-ast
---

## Parse to AST with `Markdown.Parse()` when you need structured access

Parse to AST with `Markdown.Parse()` when you need structured access: to document content (e.g., extracting links, headings, or code blocks) rather than regex on raw Markdown.
