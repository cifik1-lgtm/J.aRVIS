---
title: "Use `Matrix<double>.Build` and `Vector<double>.Build` factory methods"
impact: MEDIUM
impactDescription: "general best practice"
tags: mathnet, dotnet, general, linear-algebra-matrices, vectors, decompositions
---

## Use `Matrix<double>.Build` and `Vector<double>.Build` factory methods

Use `Matrix<double>.Build` and `Vector<double>.Build` factory methods: instead of constructors for clear, readable matrix and vector creation.
