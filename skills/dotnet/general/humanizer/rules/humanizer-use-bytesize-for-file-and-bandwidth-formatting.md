---
title: "Use `ByteSize` for file and bandwidth formatting"
impact: MEDIUM
impactDescription: "general best practice"
tags: humanizer, dotnet, general, human-readable-datetime-formatting, pluralization, number-to-words-conversion
---

## Use `ByteSize` for file and bandwidth formatting

Use `ByteSize` for file and bandwidth formatting: instead of manual division-by-1024 logic, which is error-prone and inconsistent.
