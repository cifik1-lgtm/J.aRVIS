---
title: "Use `XRect` with `XStringFormats` for text alignment"
impact: MEDIUM
impactDescription: "general best practice"
tags: pdfsharpcore, dotnet, general, creating-pdf-documents-programmatically, drawing-textshapesimages-on-pdf-pages, modifying-existing-pdfs
---

## Use `XRect` with `XStringFormats` for text alignment

Use `XRect` with `XStringFormats` for text alignment: instead of manually calculating text positions, which is error-prone and breaks with different font sizes.
