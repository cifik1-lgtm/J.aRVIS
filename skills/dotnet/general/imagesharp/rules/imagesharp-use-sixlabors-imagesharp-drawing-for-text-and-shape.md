---
title: "Use `SixLabors.ImageSharp.Drawing` for text and shape rendering"
impact: MEDIUM
impactDescription: "general best practice"
tags: imagesharp, dotnet, general, image-resizing, cropping, format-conversion
---

## Use `SixLabors.ImageSharp.Drawing` for text and shape rendering

Use `SixLabors.ImageSharp.Drawing` for text and shape rendering: rather than pixel-level manipulation, which is error-prone and not optimized.
