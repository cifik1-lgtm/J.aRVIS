---
title: "Use `ResizeMode.Max` for responsive thumbnails"
impact: MEDIUM
impactDescription: "general best practice"
tags: imagesharp, dotnet, general, image-resizing, cropping, format-conversion
---

## Use `ResizeMode.Max` for responsive thumbnails

Use `ResizeMode.Max` for responsive thumbnails: to maintain aspect ratio while fitting within a bounding box, and `ResizeMode.Crop` for fixed-size avatars.
