---
title: "Use `Image.LoadAsync` and `SaveAsync`"
impact: HIGH
impactDescription: "significant quality or reliability improvement"
tags: imagesharp, dotnet, general, image-resizing, cropping, format-conversion
---

## Use `Image.LoadAsync` and `SaveAsync`

Use `Image.LoadAsync` and `SaveAsync`: for I/O-bound operations to avoid blocking threads in web applications and worker services.
