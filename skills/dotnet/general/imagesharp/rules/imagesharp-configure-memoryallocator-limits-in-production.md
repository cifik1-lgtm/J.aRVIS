---
title: "Configure `MemoryAllocator` limits in production"
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: imagesharp, dotnet, general, image-resizing, cropping, format-conversion
---

## Configure `MemoryAllocator` limits in production

Configure `MemoryAllocator` limits in production: to prevent out-of-memory conditions when processing user-uploaded images of unknown size.
