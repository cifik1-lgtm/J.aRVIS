---
title: "Use `<Show>` and `<For>` instead of ternaries and `"
impact: MEDIUM
impactDescription: "general best practice"
tags: mitosis, design-system, mitosis-component-authoring, cross-framework-component-compilation, multi-framework-design-system-components
---

## Use `<Show>` and `<For>` instead of ternaries and `

Use `<Show>` and `<For>` instead of ternaries and `.map()` — these are the only control flow patterns that compile reliably to all targets.
