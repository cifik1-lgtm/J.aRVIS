---
title: "Run `test-storybook` in CI to catch interaction regressions..."
impact: MEDIUM
impactDescription: "general best practice"
tags: storybook, design-system, storybook-stories-csf3-and-csf-factories, play-functions, args-and-argtypes
---

## Run `test-storybook` in CI to catch interaction regressions...

Run `test-storybook` in CI to catch interaction regressions on every PR.
