---
title: "Set `$type` on groups rather than individual tokens to..."
impact: MEDIUM
impactDescription: "general best practice"
tags: design-tokens, design-system, w3c-dtcg-token-files, token-types-color, dimension
---

## Set `$type` on groups rather than individual tokens to...

Set `$type` on groups rather than individual tokens to reduce repetition.
