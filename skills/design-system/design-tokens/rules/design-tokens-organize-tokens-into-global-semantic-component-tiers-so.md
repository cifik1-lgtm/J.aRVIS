---
title: "Organize tokens into global → semantic → component tiers so..."
impact: MEDIUM
impactDescription: "general best practice"
tags: design-tokens, design-system, w3c-dtcg-token-files, token-types-color, dimension
---

## Organize tokens into global → semantic → component tiers so...

Organize tokens into global → semantic → component tiers so themes only override the semantic layer.
