---
title: "Use ESM for new projects."
impact: MEDIUM
impactDescription: "general best practice"
tags: node, typescript, runtime, nodejs-server-side-development, http-servers-and-apis, file-system-operations
---

## Use ESM for new projects.

Set `"type": "module"` in `package.json`. Use `.mjs`/`.cjs` extensions only when mixing module systems within a single package.
