---
title: "Use `fs/promises` instead of callback-based `fs`."
impact: HIGH
impactDescription: "significant quality or reliability improvement"
tags: node, typescript, runtime, nodejs-server-side-development, http-servers-and-apis, file-system-operations
---

## Use `fs/promises` instead of callback-based `fs`.

The promise-based API integrates naturally with async/await and avoids callback hell.
