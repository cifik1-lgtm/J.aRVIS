---
title: "Use `deno compile` for distribution"
impact: MEDIUM
impactDescription: "general best practice"
tags: deno, typescript, runtime, deno-runtime-projects, typescript-first-server-side-development, secure-sandboxed-execution
---

## Use `deno compile` for distribution

Use `deno compile` for distribution: compile your application to a self-contained executable for easy deployment without requiring the Deno runtime on the target machine.
