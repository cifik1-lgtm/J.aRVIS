---
title: "Use `deno.json` for project configuration"
impact: MEDIUM
impactDescription: "general best practice"
tags: deno, typescript, runtime, deno-runtime-projects, typescript-first-server-side-development, secure-sandboxed-execution
---

## Use `deno.json` for project configuration

Use `deno.json` for project configuration: centralize imports, tasks, compiler options, and tool settings in a single configuration file.
