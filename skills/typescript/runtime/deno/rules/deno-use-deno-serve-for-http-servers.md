---
title: "Use `Deno.serve` for HTTP servers"
impact: MEDIUM
impactDescription: "general best practice"
tags: deno, typescript, runtime, deno-runtime-projects, typescript-first-server-side-development, secure-sandboxed-execution
---

## Use `Deno.serve` for HTTP servers

Use `Deno.serve` for HTTP servers: it provides automatic parallelism, follows web standards, and is the recommended HTTP server API.
