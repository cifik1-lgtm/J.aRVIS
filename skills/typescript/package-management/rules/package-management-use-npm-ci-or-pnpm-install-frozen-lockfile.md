---
title: "Use `npm ci` (or `pnpm install --frozen-lockfile`)"
impact: MEDIUM
impactDescription: "general best practice"
tags: package-management, typescript, package-manager-selection, npmyarnpnpmbun-configuration, monorepo-workspaces
---

## Use `npm ci` (or `pnpm install --frozen-lockfile`)

Use `npm ci` (or `pnpm install --frozen-lockfile`): in CI pipelines for deterministic installs.
