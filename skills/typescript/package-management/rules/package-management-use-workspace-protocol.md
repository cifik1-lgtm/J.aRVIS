---
title: "Use `workspace:*` protocol"
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: package-management, typescript, package-manager-selection, npmyarnpnpmbun-configuration, monorepo-workspaces
---

## Use `workspace:*` protocol

(pnpm/yarn) for internal package references so they always resolve to the local version.
