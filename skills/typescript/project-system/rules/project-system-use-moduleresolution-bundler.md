---
title: "Use `\"moduleResolution\": \"bundler\"`"
impact: MEDIUM
impactDescription: "general best practice"
tags: project-system, typescript, tsconfigjson-configuration, compiler-options, build-tool-selection
---

## Use `"moduleResolution": "bundler"`

Use `"moduleResolution": "bundler"`: for frontend projects and `"NodeNext"` for Node.js libraries that will be consumed directly without a bundler.
