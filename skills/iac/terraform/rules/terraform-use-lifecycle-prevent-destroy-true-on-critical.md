---
title: "Use `lifecycle { prevent_destroy = true }` on critical..."
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: terraform, iac, multi-cloud-provisioning, hcl-configuration, terraform-state-management
---

## Use `lifecycle { prevent_destroy = true }` on critical...

Use `lifecycle { prevent_destroy = true }` on critical stateful resources.
