---
title: "Use Bicep instead of ARM JSON"
impact: MEDIUM
impactDescription: "general best practice"
tags: bicep, iac, azure-resource-deployment, bicep-dsl-modules, what-if-previews
---

## Use Bicep instead of ARM JSON

Use Bicep instead of ARM JSON — it compiles to identical output with better readability.
