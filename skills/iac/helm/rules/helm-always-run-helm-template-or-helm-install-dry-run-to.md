---
title: "Always run `helm template` or `helm install --dry-run` to..."
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: helm, iac, helm-charts, kubernetes-package-management, go-templates
---

## Always run `helm template` or `helm install --dry-run` to...

Always run `helm template` or `helm install --dry-run` to preview rendered manifests before deploying.
