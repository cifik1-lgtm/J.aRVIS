---
title: "Use `helm lint` in CI to catch template errors early"
impact: MEDIUM
impactDescription: "general best practice"
tags: helm, iac, helm-charts, kubernetes-package-management, go-templates
---

## Use `helm lint` in CI to catch template errors early

Use `helm lint` in CI to catch template errors early.
