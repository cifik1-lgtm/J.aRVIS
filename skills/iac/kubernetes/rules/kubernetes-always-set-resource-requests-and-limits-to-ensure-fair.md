---
title: "Always set resource `requests` and `limits` to ensure fair..."
impact: CRITICAL
impactDescription: "essential for correctness or security"
tags: kubernetes, iac, kubernetes-manifests, deployments, services
---

## Always set resource `requests` and `limits` to ensure fair...

Always set resource `requests` and `limits` to ensure fair scheduling and prevent noisy neighbors.
