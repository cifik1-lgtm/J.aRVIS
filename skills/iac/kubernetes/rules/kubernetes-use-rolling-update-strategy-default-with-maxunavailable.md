---
title: "Use rolling update strategy (default) with `maxUnavailable`..."
impact: MEDIUM
impactDescription: "general best practice"
tags: kubernetes, iac, kubernetes-manifests, deployments, services
---

## Use rolling update strategy (default) with `maxUnavailable`...

Use rolling update strategy (default) with `maxUnavailable` and `maxSurge` for zero-downtime deploys.
