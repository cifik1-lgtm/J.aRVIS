---
title: "Use `kubectl diff` before `kubectl apply` to review changes"
impact: MEDIUM
impactDescription: "general best practice"
tags: kubernetes, iac, kubernetes-manifests, deployments, services
---

## Use `kubectl diff` before `kubectl apply` to review changes

Use `kubectl diff` before `kubectl apply` to review changes.
